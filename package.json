{
  "name": "testpkg",
  "version": "1.0.0",
  "description": "just for test",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "mulianju",
  "license": "ISC",
  "devDependencies": {
    "assert": "^2.0.0",
    "mocha": "^10.0.0"
  }
}
